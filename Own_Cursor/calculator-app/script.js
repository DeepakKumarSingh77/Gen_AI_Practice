const display = document.querySelector(".calculator-screen"); let currentInput = "0"; let operator = null; let prevInput = null; let resetScreen = false; document.querySelector(".calculator-keys").addEventListener("click", (e) => { const { value } = e.target; if (!value) return; if (value === "all-clear") { currentInput = "0"; operator = null; prevInput = null; } else if (value === "=") { if (operator && prevInput !== null) { currentInput = eval(`${prevInput}${operator}${currentInput}`).toString(); operator = null; prevInput = null; } } else if (["+", "-", "*", "/"].includes(value)) { prevInput = currentInput; operator = value; resetScreen = true; } else { if (currentInput === "0" || resetScreen) { currentInput = value; resetScreen = false; } else { currentInput += value; } } display.value = currentInput; });
